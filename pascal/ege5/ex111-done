###
uses school;
var s := 0;
var s2 :=  0;
var a:=|1,2,4,5,6,$B|;
for var i:=1 to a.cnt do
  begin
  if i mod 2 = 0 then
    s += (tobase(a[i], 12)).toi
  else 
    s2 += (tobase(a[i], 12)).toi;
   end;
