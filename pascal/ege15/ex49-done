###
uses school;
var p := range(10, 20);
var q := range(15, 25);
var a := range(8, 17);
var f := true;
for var x := 1 to 1000 do
  if (( (x in p) <= (x in q) ) or (x in a)) = false then
  begin
    f := false;
    break
  end;
if f then
  pr(1)
else 
  pr(0)
