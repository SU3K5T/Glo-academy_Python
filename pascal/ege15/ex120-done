###
uses school;
for var a:=1 to 1000 do
begin
  var f := 0;
  for var x := 0 to 1000 do
    if (x mod a <> 0) and (x mod 6 = 0)  <= (x mod 3 <> 0) = false then
    begin
      f := 1;
      break;
    end;
    if f = 0 then
      pr(a)
end;  
