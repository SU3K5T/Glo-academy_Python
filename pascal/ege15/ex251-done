###
uses school;
  var k := 0;
for var a := 1 to 1000 do
begin
  var f := true;
  for var x := 1 to 1000 do
  for var y := 1 to 1000 do
    if ( (y*y < a) <= (y <= 8) ) and ( (x <= 5) <= (x*x <= A) ) = false then
      begin
      f := false; 
       break
    end;
    if f then
      k += 1;
end;
print(k)
