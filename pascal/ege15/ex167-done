//167
###
uses school;
for var a := 1 to 1000 do
begin
  var flag := 0;
  for var x := 1 to 1000 do
    if ( (x and 28 <>  0) or  (x and 45 <> 0)) <= ((x and 17 = 0) <= (x and A <> 0)) = false then
    begin
      flag := 1;
      break
    end;
    if flag = 0 then
      pr(a)
end;
