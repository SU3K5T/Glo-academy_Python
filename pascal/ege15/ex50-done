###
uses school;
var p := range(10, 40);
var q := range(5, 15);
var r := range(35,50);
var a := range(120, 130);
var f := true;
for var x := 1 to 1000 do
  if (( (x in p) <= (x in q) ) or ( (x in A) <= (x in r) )) = false then
  begin
    f := false;
    break
  end;
  if f then
    pr(1)
  else 
    pr(0)
