###
uses school;
var p := range(10,25);
var q := range(0,12);
var a := range(12,40);
var f := true;
for var x := 1 to 1000 do
  if ((not(x in a) <= not(x in p)) or (x in q)) = false then
  begin
       f := false;
       break
  end;
  if f then
    pr('yes')
  else 
    pr('no')
