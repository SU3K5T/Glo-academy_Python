###
uses school;
var s := '5' * 200;
while ('555' in s) or ('333' in s) do
  if ('555' in s) then
    s := s.Replace('555','3',1)
  else 
    s := s.Replace('333', '5' , 1);
var s2 := s.ToInteger;
pr(Digits(s2).sum)
